function setup() {
    // Create canvas for top container
    let topCanvas = createCanvas(800, 300); // Adjust size as needed
    topCanvas.parent("top-container");
    background(220);
  
    // Create canvas for bottom container
    let bottomCanvas = createCanvas(800, 300); // Adjust size as needed
    bottomCanvas.parent("bottom-container");
    background(220);
  }
  
  function draw() {
    // Your draw function code here
  }
  